heal-enabled=true
recovery-tries=3  # Number of healing attempts
screenshot-enabled=true  # Capture screenshots on failure
serverHost=localhost  # Default; change if using remote server
serverPort=8087
